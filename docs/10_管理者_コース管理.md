# 10 管理者：コース管理

## 概要

URL：`/admin/courses`  
管理者がコースの追加・編集・削除を行う画面。

---

## ToDo

### 一覧
- [x] `app/admin/courses/page.tsx` を作成
- [x] 全コースをテーブル形式で一覧表示
- [x] 各コースに「編集」「削除」ボタンを設置
- [x] 「新規コース作成」ボタンを設置

### 新規作成
- [x] `app/admin/courses/new/page.tsx` を作成
- [x] コース作成フォームを実装（タイトル・説明・サムネイル URL）
- [x] `createCourse` Server Action を実装
  - [x] `requireAdmin()` でアクセス制御
  - [x] DB insert 後 `revalidatePath` → `redirect`

### 編集
- [x] `app/admin/courses/[id]/edit/page.tsx` を作成
- [x] 既存データをフォームの初期値に設定
- [x] `updateCourse` Server Action を実装
  - [x] `requireAdmin()` でアクセス制御
  - [x] DB update 後 `revalidatePath` → `redirect`

### 削除
- [x] `deleteCourse` Server Action を実装
  - [x] `requireAdmin()` でアクセス制御
  - [x] DB delete 後 `revalidatePath`
  - [x] `lessons` は `on delete cascade` で自動削除される

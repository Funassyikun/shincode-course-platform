# 09 管理者ガード

## 概要

`/admin` 配下へのアクセスを、環境変数 `ADMIN_EMAIL` と一致するログイン済みユーザーのみに制限する。

---

## ToDo

- [x] 管理者チェック用のユーティリティ関数 `requireAdmin()` を `lib/auth.ts` に実装
- [x] `app/admin/layout.tsx` を作成し、管理者チェックを行う
  - [x] 未ログインの場合 → `/login` へ redirect
  - [x] ログイン済みだが `ADMIN_EMAIL` と不一致の場合 → `/` へ redirect
- [x] `app/admin/page.tsx` を作成（管理者トップ）
  - [x] コース管理・レッスン管理へのナビゲーションリンク

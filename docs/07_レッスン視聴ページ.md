# 07 レッスン視聴���ージ

## 概要

URL：`/courses/[id]/lessons/[lessonId]`  
YouTube 動画を埋め込みで再生する。  
`is_free = false` のレッスンには認証が必要。未ログインユーザーはログインページへリダイレクト。

---

## ToDo

- [x] `app/courses/[id]/lessons/[lessonId]/page.tsx` を作成（Server Component）
- [x] `getLessonById(lessonId)` をデータ取得関数として実装
- [x] アクセス制御ロジックを実装
  - [x] `is_free = false` ���つ未ログインの場合 → `/login?next=現在のURL` へ redirect
  - [x] `is_free = true` は誰でも視聴可
- [x] YouTube 動画を埋め込みで表示
  - [x] `youtube_url` から動画 ID を抽出して `<iframe>` に変換
  - [x] アスペクト比 16:9 をレスポンシブに維持
- [x] レッスンタイトルを表示
- [x] 前のレッスン・次のレッスンへのナビゲーションリンクを��示
- [x] 存在しない `lessonId` ��場合は `notFound()` を呼ぶ
- [x] `loading.tsx` でスケルトン UI を実装

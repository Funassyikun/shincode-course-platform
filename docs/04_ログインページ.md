# 04 ログインページ

## 概要

URL：`/login`  
Google アカウントでのログイン画面。未ログインユーザーが保護されたレッスンにアクセスした場合もここへリダイレクトされる。

---

## ToDo

- [x] `app/login/page.tsx` を作成
- [x] Google ログインボタンを配置
- [x] `signInWithGoogle` Server Action を実装（`app/lib/actions/auth.ts`）
- [x] `signInWithOAuth` で Google プロバイダーを指定し、コールバック URL を設定
- [x] ログイン済みユーザーが `/login` にアクセスした場合は `/` へリダイレクト
- [x] ログイン後の遷移先（`next` クエリパラメータ）を考慮したリダイレクト処理
- [x] レスポンシブ対応（モバイル・PC）
- [x] ローディング状態の UI を実装

---

## 実装メモ

### Server Action（`app/lib/actions/auth.ts`）

```ts
'use server'

import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'

export async function signInWithGoogle(next?: string) {
  const supabase = await createClient()
  const { data, error } = await supabase.auth.signInWithOAuth({
    provider: 'google',
    options: {
      redirectTo: `${process.env.NEXT_PUBLIC_SITE_URL}/auth/callback?next=${next ?? '/'}`,
    },
  })
  if (data.url) redirect(data.url)
}
```

### ページ（`app/login/page.tsx`）

```tsx
import { createClient } from '@/lib/supabase/server'
import { redirect } from 'next/navigation'
import { signInWithGoogle } from '@/app/lib/actions/auth'

export default async function LoginPage({
  searchParams,
}: PageProps<'/login'>) {
  const { next } = await searchParams

  // ログイン済みなら / へ
  const supabase = await createClient()
  const { data } = await supabase.auth.getClaims()
  if (data?.claims) redirect('/')

  return (
    <main>
      <h1>ログイン</h1>
      <form action={signInWithGoogle.bind(null, next)}>
        <button type="submit">Google でログイン</button>
      </form>
    </main>
  )
}
```

### アクセス制御の流れ

```
未ログインユーザーが /courses/[id]/lessons/[lessonId] にアクセス
  → is_free = false のレッスン
  → /login?next=/courses/[id]/lessons/[lessonId] へリダイレクト
  → ログイン後、next の URL へリダイレクト
```

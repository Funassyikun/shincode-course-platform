# 08 視聴進捗トラッキング

## 概要

ログイン済みユーザーがレッスンを「視聴済み」にマークできる機能。  
`lesson_progress` テーブルに記録し、コース詳細ページで進捗率として表示する。

---

## ToDo

- [x] `toggleLessonProgress` Server Action を実装
  - [x] 未完了 → INSERT（視聴済みにする）
  - [x] 完了済み → DELETE（未視聴に戻す）
  - [x] 認証チェック（未ログインは実行不可）
  - [x] `revalidatePath` でコース詳細ページのキャッシュを更新
- [x] レッスン視聴ページに「視聴済みにする」ボタンを追加（Client Component）
  - [x] 現在の完了状態を反映したトグルボタン
  - [x] `useActionState` でペンディング状態を表示
- [x] コース詳細ページ（06）の進捗率表示と連動していることを確認
- [x] `lesson_progress` の RLS が正しく機能しているか確認（02 で設定済み）

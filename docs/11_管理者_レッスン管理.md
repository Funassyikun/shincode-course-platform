# 11 管理者：レッスン管理

## 概要

URL：`/admin/courses/[id]/lessons`  
管理者が特定コースのレッスンの追加・編集・削除・並び順変更を行う画面。

---

## ToDo

### 一覧
- [x] `app/admin/courses/[id]/lessons/page.tsx` を作成
- [x] 該当コースのレッスンを `order` 昇順でテーブル表示
- [x] 各レッスンに「編集」「削除」ボタンを設置
- [x] 「新規レッスン追加」ボタンを設置

### 新規作成
- [x] `app/admin/courses/[id]/lessons/new/page.tsx` を作成
- [x] レッスン作成フォームを実装（タイトル・YouTube URL・`is_free`・並び順）
- [x] `createLesson` Server Action を実装
  - [x] `requireAdmin()` でアクセス制御
  - [x] `order` は既存レッスンの最大値 + 1 を自動設定
  - [x] DB insert 後 `revalidatePath` → `redirect`

### 編集
- [x] `app/admin/courses/[id]/lessons/[lessonId]/edit/page.tsx` を作成
- [x] 既存データをフォームの初期値に設定
- [x] `updateLesson` Server Action を実装
  - [x] `requireAdmin()` でアクセス制御
  - [x] DB update 後 `revalidatePath` → `redirect`

### 削除
- [x] `deleteLesson` Server Action を実装
  - [x] `requireAdmin()` でアクセス制御
  - [x] DB delete 後 `revalidatePath`
  - [x] `lesson_progress` は `on delete cascade` で自動削除される

### 並び順変更
- [x] `moveLessonUp` / `moveLessonDown` Server Action を実装
- [x] 上へ／下へ移動ボタンで並び順を変更できるようにする

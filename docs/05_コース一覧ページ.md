# 05 コース一覧ページ

## 概要

URL：`/`（トップページ）  
公開中のコースをカード形式で一覧表示する。認証不要。

---

## ToDo

- [x] `app/page.tsx` を実装（Server Component）
- [x] `courses` テーブルから全件取得するデータ取得関数を `lib/data.ts` に実装
- [x] `CourseCard` コンポーネントを `app/ui/course-card.tsx` に作成
  - [x] コースタイトルを表示
  - [x] サムネイル画像を表示（`next/image` を使用）
  - [x] コース詳細ページへの `<Link>` を設置
- [x] コース一覧のグリッドレイアウトを実装（レスポンシブ対応）
- [x] `loading.tsx` でスケルトン UI を実装
- [x] コースが 0 件の場合の空状態を表示

---

## 実装メモ

### データ取得（`lib/data.ts`）

```ts
import 'server-only'
import { createClient } from '@/lib/supabase/server'

export async function getCourses() {
  const supabase = await createClient()
  const { data, error } = await supabase
    .from('courses')
    .select('id, title, description, thumbnail_url, created_at')
    .order('created_at', { ascending: false })
  if (error) throw error
  return data
}
```

### ページ（`app/page.tsx`）

```tsx
import { getCourses } from '@/lib/data'
import CourseCard from '@/app/ui/course-card'

export default async function Home() {
  const courses = await getCourses()

  return (
    <main>
      <h1>コース一覧</h1>
      {courses.length === 0 ? (
        <p>現在公開中のコースはありません。</p>
      ) : (
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
          {courses.map((course) => (
            <CourseCard key={course.id} course={course} />
          ))}
        </div>
      )}
    </main>
  )
}
```

### コンポーネント構成

```
app/
  page.tsx              # コース一覧（Server Component）
  loading.tsx           # スケルトン UI
  ui/
    course-card.tsx     # コースカード（Server Component）
lib/
  data.ts               # getCourses()
```

# 06 コース詳細ページ

## 概要

URL：`/courses/[id]`  
コースの説明・レッスン一覧・ログイン済みユーザーの進捗率を表示する。認証不要。

---

## ToDo

- [x] `app/courses/[id]/page.tsx` を作成（Server Component）
- [x] `getCourseById(id)` をデータ取得関数として実装
- [x] `getLessonsByCourseId(courseId)` を実装（`order` 昇順でソート）
- [x] コース情報（タイトル・説明・サムネイル）を表示
- [x] レッスン一覧を番号付きリストで表示
  - [x] 各レッスンへの `<Link>` を設置
  - [x] `is_free = false` かつ未ログインの場合はロックアイコンを表示
- [x] ログイン済みユーザー向けの進捗率を表示（例：3 / 10 本完了）
  - [x] `lesson_progress` から該当コースの完了数を取得
  - [x] 未ログインユーザーには進捗を表示しない
- [x] `loading.tsx` でスケルトン UI を実装
- [x] 存在しない `id` の場合は `notFound()` を呼ぶ
